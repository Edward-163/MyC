#include <string.h>
#include <stdlib.h>
#include <stdio.h>
#include <math.h>
#include <ctype.h>
#include <stdbool.h>
#include "uthash.h"

/*
 给一个锚点(posX,posY)，一个数组arr=[[x1,y1],[x2,y2],…]，在数组arr中找到能与锚点匹配的点，匹配的规则是：posX=xi，或者posY=yi，将所有匹配的点存放在一起，在其中找到与锚点曼哈顿距离最近的点的个数，曼哈顿距离的定义为：|xi-xj|+|yi-yj|，输出该个数。
例：输入：锚点(15,20)，
	 数组[[35,20],[30,10],[20,23],[15,30],[20,15],[25,20]];

 */

int main() {

    return 0;
}